# php_install

Checks for currently installed version of PHP.

Adds the ppa:ondrej repoository to /etc/apt/sources.list

Then gives the option of which version of PHP you want to install and have set as the default version on your server.
Options are php5.6 up to the most current version php8.0.
